# suppress inspection "UnusedProperty" for whole file
# Properties
middle.tier.db.name=Middle Tier Money DB Server (DEBUG)
server.port= 10017

middle.tier.money.db.url=${db.dev.money.server}?autoReconnect=true&useSSL=false
middle.tier.money.db.username=${db.dev.money.user}
middle.tier.money.db.password=${db.dev.money.password}

spring.datasource.tomcat.test-while-idle=true
spring.datasource.tomcat.testOnBorrow=true
spring.datasource.tomcat.validationQuery=SELECT 1

middle.tier.service.name=middletier-money-dev

management.endpoint.health.show-details=always

info.app.name=Money DB (DEV)
info.app.description=Used for managing transactions (DEV)
info.app.version=@version@

spring.application.name=Middle Tier Money (Develop)
spring.boot.admin.client.url=${spring.admin.clienturl}
management.security.enabled=false
management.endpoints.web.exposure.include=*
